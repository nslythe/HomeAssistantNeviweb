language: python
python:
  - "3.3"
  - "3.4"
install:
  - pip install coveralls
  - pip install sphinx
# command to run tests
script:
  - coverage run --source=sinope test.py
  - cd doc
  - sphinx-build -b html . ../build/doc/
after_success:
  - coveralls
